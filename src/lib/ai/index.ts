/**
 * Public entry point for the AI data layer. Import the summary client from
 * `@/lib/ai` rather than reaching into individual modules.
 */
export { requestSummary } from './requestSummary';
export type { SummaryResult } from './requestSummary';
